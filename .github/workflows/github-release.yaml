name: github-release

on:
  push:
    branches:
      - '**'
    tags:
      - 'v*'
  pull_request:

jobs:
  build_and_release:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
        - os: macos-latest
          TARGET: x86_64-apple-darwin
          TITLE: macos-amd64
        - os: ubuntu-latest
          TARGET: x86_64-unknown-linux-gnu
          TITLE: linux-amd64
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - run: echo ${{matrix.TARGET}}
      - name: Get sfn-ng Version
        run: |
          echo "::set-output name=version::$(cat Cargo.toml | grep \"^version\" | awk '{ print $3; }' | tr -d '\"')\n"
        id: s_version
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
#          target: ${{matrix.TARGET}}
          override: true
      - uses: actions-rs/cargo@v1
        with:
#          use-cross: true
          command: build
          args: --release --target ${{matrix.TARGET}}
          # target/release/sfn-ng
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
        with:
          tag_name: ${{matrix.TITLE}}-${{steps.s_version.outputs.version}}
          release_name: Release ${{steps.s_version.outputs.version}} for ${{matrix.TITLE}}
          draft: false
          prerelease: false
      - name: Upload Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
          asset_path: ./target/release/sfn-ng
          asset_name: sfn-ng-${{matrix.TITLE}}
          asset_content_type: application/octet-stream